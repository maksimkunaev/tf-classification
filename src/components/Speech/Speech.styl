.button {
  padding: 10px 30px;
  border-radius: 5px;
  background-image: linear-gradient(45deg, #141ae2, #080806a8);
  outline: none;
  cursor: pointer;
  margin: 0 5px;

  border: 1px solid #fff;
  font-weight: 300;
  font-size: 16px;
  color: #fff;
  background: transparent;
}

.speech {
  color: #fff;
}
